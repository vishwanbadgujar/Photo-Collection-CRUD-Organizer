# Photo-Collection-CRUD-Organizer
The Photo Collection Organizer is a CLI Python app that manages photo metadata (Title, Tags, Location) using the four CRUD functions (Create, Read, Update, Delete). It uses JSON for permanent data storage.
